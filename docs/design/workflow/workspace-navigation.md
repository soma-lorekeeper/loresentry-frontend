# 작업공간 콘텐츠 열기 Workflow

```text
통합 작업공간 사이드바
→ 콘텐츠 선택
→ 현재 탭이 새 탭 화면이면 해당 탭에 열기
→ 새 탭 화면이 아니면 새 탭으로 열기
```

- 이미 열려 있는 콘텐츠를 선택하면 해당 탭으로 이동한다.
- 즐겨찾기 바로가기를 선택해도 원본 파일과 같은 탭을 사용한다.
- 폴더는 탭으로 열지 않고 사이드바에서 펼치거나 접는다.
- 프로젝트에 다시 진입하면 마지막으로 열어 둔 탭을 복원한다.
- 모든 콘텐츠 탭을 닫거나 복원할 탭이 없으면 새 탭 화면 하나를 연다.
